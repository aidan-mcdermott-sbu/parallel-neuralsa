# Edit this file to change the Learning PI / C comparison TSP experiment.
# The runner reads these values and launches scripts/main.py once per combo.

experiment: tsp_ppo
output_root: ./outputs/learn_pi_c_tsp_comparison

problem:
  device: cpu
  n_problems: 16
  problem_dim: 20
  embed_dim: 16

training:
  n_epochs: 40
  ppo_epochs: 2
  batch_size: 32

sa:
  outer_steps: 400
  inner_steps: 1
  c: 0.1
  n_chains: 5

combos:
  - name: learn_pi_learn_c
    learn_policy: true
    learn_c: true
  - name: learn_pi_no_c
    learn_policy: true
    learn_c: false
